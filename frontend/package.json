{
  "name": "frontend-web",
  "version": "1.0.0",
  "description": "Frontend com Live-Server",
  "scripts": {
    "dev": "live-server --port=8080 --host=0.0.0.0 --wait=1000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}